//package awesomemod.crafting
//
//case class Item(a: Int, b: Int, c: Int)
//class Store(item: Item, luck: Int)
//class Example(stores: Array[Store], stores2: Array[Store]) {
//  def asd(items: Array[Item]): Unit = {
//    test2 && items.exists(inAnyStore)
//  }
//
//  def inAnyStore(item: Item): Boolean =
//    storeItemMatch(item, stores) || storeItemMatch(item, stores2)
//
//  def storeItemMatch(item: Item, storeArray: Array[Store]): Boolean =
//    storeArray.exists { store =>
//      testItems(store.item, item)
//    }
//
//  def testItems(one: Item, two: Item): Boolean = {
//    one.a == two.a && one.b == two.b
//  }
//
//  def test2: Boolean = ???
//
//
//  def hasIntersection(a:Array[Int], b:Array[Int]): Boolean = {
//    ???
//  }
//
//}
